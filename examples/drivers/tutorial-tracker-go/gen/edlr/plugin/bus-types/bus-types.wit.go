// Code generated by wit-bindgen-go. DO NOT EDIT.

// Package bustypes represents the imported interface "edlr:plugin/bus-types@0.4.0".
//
// `bus-error` を関数を持たない型だけの interface に切り出したもの。
// `bus` や `bus-host` のように関数を持つ interface から `use` で型を借りると、
// その interface 全体が import 先の world に引き込まれてしまう
// (WIT の `use` は型だけでなく interface 全体への依存を生む)。
// `world driver` が `bus-host` を import しつつ `bus` を引き込まないためには、
// 両者が共有する型をこの関数なし interface から借りる必要がある。
package bustypes

import (
	"go.bytecodealliance.org/cm"
)

// BusError represents the variant "edlr:plugin/bus-types@0.4.0#bus-error".
//
//	variant bus-error {
//		permission-denied(string),
//		unknown-driver(string),
//		unknown-topic(string),
//		driver-unavailable(string),
//		queue-full(string),
//		too-large(string),
//	}
type BusError cm.Variant[uint8, string, string]

// BusErrorPermissionDenied returns a [BusError] of case "permission-denied".
func BusErrorPermissionDenied(data string) BusError {
	return cm.New[BusError](0, data)
}

// PermissionDenied returns a non-nil *[string] if [BusError] represents the variant case "permission-denied".
func (self *BusError) PermissionDenied() *string {
	return cm.Case[string](self, 0)
}

// BusErrorUnknownDriver returns a [BusError] of case "unknown-driver".
func BusErrorUnknownDriver(data string) BusError {
	return cm.New[BusError](1, data)
}

// UnknownDriver returns a non-nil *[string] if [BusError] represents the variant case "unknown-driver".
func (self *BusError) UnknownDriver() *string {
	return cm.Case[string](self, 1)
}

// BusErrorUnknownTopic returns a [BusError] of case "unknown-topic".
func BusErrorUnknownTopic(data string) BusError {
	return cm.New[BusError](2, data)
}

// UnknownTopic returns a non-nil *[string] if [BusError] represents the variant case "unknown-topic".
func (self *BusError) UnknownTopic() *string {
	return cm.Case[string](self, 2)
}

// BusErrorDriverUnavailable returns a [BusError] of case "driver-unavailable".
func BusErrorDriverUnavailable(data string) BusError {
	return cm.New[BusError](3, data)
}

// DriverUnavailable returns a non-nil *[string] if [BusError] represents the variant case "driver-unavailable".
func (self *BusError) DriverUnavailable() *string {
	return cm.Case[string](self, 3)
}

// BusErrorQueueFull returns a [BusError] of case "queue-full".
func BusErrorQueueFull(data string) BusError {
	return cm.New[BusError](4, data)
}

// QueueFull returns a non-nil *[string] if [BusError] represents the variant case "queue-full".
func (self *BusError) QueueFull() *string {
	return cm.Case[string](self, 4)
}

// BusErrorTooLarge returns a [BusError] of case "too-large".
func BusErrorTooLarge(data string) BusError {
	return cm.New[BusError](5, data)
}

// TooLarge returns a non-nil *[string] if [BusError] represents the variant case "too-large".
func (self *BusError) TooLarge() *string {
	return cm.Case[string](self, 5)
}

var _BusErrorStrings = [6]string{
	"permission-denied",
	"unknown-driver",
	"unknown-topic",
	"driver-unavailable",
	"queue-full",
	"too-large",
}

// String implements [fmt.Stringer], returning the variant case name of v.
func (v BusError) String() string {
	return _BusErrorStrings[v.Tag()]
}
