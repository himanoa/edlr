[workspace]

[package]
name = "http-caller"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
edlr-plugin-sdk = { path = "../../../sdk/rust" }
serde_json = "1"

[profile.release]
opt-level = "s"
