[workspace]

[package]
name = "state-reader"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
edlr-plugin-sdk = { path = "../../../sdk/rust" }

[profile.release]
opt-level = "s"
