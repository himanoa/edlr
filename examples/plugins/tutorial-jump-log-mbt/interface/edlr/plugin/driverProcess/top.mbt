// Generated by `wit-bindgen` 0.45.1. DO NOT EDIT!

pub(all) enum InstanceState {
      RUNNING; EXITED
} derive(Show, Eq)

pub fn InstanceState::ordinal(self : InstanceState) -> Int {
      match self {
            RUNNING => 0
            EXITED => 1
      }
}

pub fn InstanceState::from(self : Int) -> InstanceState {
      match self {
            0 => RUNNING
            1 => EXITED
            _ => panic()
      }
}

pub(all) struct Instance {
      index : UInt; port : UInt; state : InstanceState; exit_code : Int?
} derive(Show, Eq)

pub(all) enum DriverError {
      PermissionDenied(String)
      NotConfigured(String)
      UnknownSidecar(String)
      RateLimited(String)
      SpawnFailed(String)
} derive(Show, Eq)

pub fn ensure_started(name : String) -> Result[Array[Instance], DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportEnsureStarted(@ffi.str2ptr(name), name.length(), return_area);

      let lifted23 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let array : Array[Instance] = [];
                  for index = 0; index < (@ffi.load32((return_area) + 8)); index = index + 1 {
                        let base1 = (@ffi.load32((return_area) + 4)) + (index * 16)

                        let lifted : Int? = match @ffi.load8_u((base1) + 8) {
                              0 => Option::None
                              1 => {

                                    Option::Some(@ffi.load32((base1) + 12))
                              }
                              _ => panic()
                        }

                        array.push(Instance::{index : (@ffi.load32((base1) + 0)).reinterpret_as_uint(), port : (@ffi.load16_u((base1) + 4).land(0xFFFF).reinterpret_as_uint()), state : InstanceState::from(@ffi.load8_u((base1) + 6)), exit_code : lifted})
                  }
                  @ffi.free(@ffi.load32((return_area) + 4))

                  Result::Ok(array)
            }
            1 => {

                  let lifted22 = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result)
                        }
                        1 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result12)
                        }
                        2 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownSidecar(result15)
                        }
                        3 => {

                              let result18 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::RateLimited(result18)
                        }
                        4 => {

                              let result21 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::SpawnFailed(result21)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted22)
            }
            _ => panic()
      }
      ignore(name)
      @ffi.free(return_area)
      return lifted23

}

pub fn stop(name : String) -> Result[Unit, DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportStop(@ffi.str2ptr(name), name.length(), return_area);

      let lifted16 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result)
                        }
                        1 => {

                              let result6 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result6)
                        }
                        2 => {

                              let result9 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownSidecar(result9)
                        }
                        3 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::RateLimited(result12)
                        }
                        4 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::SpawnFailed(result15)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(name)
      @ffi.free(return_area)
      return lifted16

}

pub fn status(name : String) -> Result[Array[Instance], DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportStatus(@ffi.str2ptr(name), name.length(), return_area);

      let lifted23 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let array : Array[Instance] = [];
                  for index = 0; index < (@ffi.load32((return_area) + 8)); index = index + 1 {
                        let base1 = (@ffi.load32((return_area) + 4)) + (index * 16)

                        let lifted : Int? = match @ffi.load8_u((base1) + 8) {
                              0 => Option::None
                              1 => {

                                    Option::Some(@ffi.load32((base1) + 12))
                              }
                              _ => panic()
                        }

                        array.push(Instance::{index : (@ffi.load32((base1) + 0)).reinterpret_as_uint(), port : (@ffi.load16_u((base1) + 4).land(0xFFFF).reinterpret_as_uint()), state : InstanceState::from(@ffi.load8_u((base1) + 6)), exit_code : lifted})
                  }
                  @ffi.free(@ffi.load32((return_area) + 4))

                  Result::Ok(array)
            }
            1 => {

                  let lifted22 = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result)
                        }
                        1 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result12)
                        }
                        2 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownSidecar(result15)
                        }
                        3 => {

                              let result18 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::RateLimited(result18)
                        }
                        4 => {

                              let result21 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::SpawnFailed(result21)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted22)
            }
            _ => panic()
      }
      ignore(name)
      @ffi.free(return_area)
      return lifted23

}

