// Generated by `wit-bindgen` 0.45.1. DO NOT EDIT!
/// 行末コメントは使わない。wit-bindgen-go が各コメントを「次のフィールド」に
/// 付けてしまい、生成物のコメントが 1 つずつずれるため(下も同じ)。
pub(all) struct Entry {
      path : String; size : UInt64; modified : UInt64?
} derive(Show, Eq)

pub(all) enum DriverError {
      PermissionDenied(String)
      NotConfigured(String)
      UnknownRoot(String)
      InvalidPath(String)
      NotFound(String)
      TooLarge(String)
      Io(String)
} derive(Show, Eq)

pub fn read(root : String, path : String) -> Result[FixedArray[Byte], DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportRead(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(path), path.length(), return_area);

      let lifted23 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let result = @ffi.ptr2bytes(@ffi.load32((return_area) + 4), @ffi.load32((return_area) + 8))

                  Result::Ok(result)
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result4 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result4)
                        }
                        1 => {

                              let result7 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result7)
                        }
                        2 => {

                              let result10 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownRoot(result10)
                        }
                        3 => {

                              let result13 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::InvalidPath(result13)
                        }
                        4 => {

                              let result16 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotFound(result16)
                        }
                        5 => {

                              let result19 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::TooLarge(result19)
                        }
                        6 => {

                              let result22 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::Io(result22)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(root)
      ignore(path)
      @ffi.free(return_area)
      return lifted23

}

pub fn read_range(root : String, path : String, offset : UInt64, len : UInt) -> Result[FixedArray[Byte], DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportReadRange(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(path), path.length(), (offset).reinterpret_as_int64(), (len).reinterpret_as_int(), return_area);

      let lifted23 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let result = @ffi.ptr2bytes(@ffi.load32((return_area) + 4), @ffi.load32((return_area) + 8))

                  Result::Ok(result)
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result4 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result4)
                        }
                        1 => {

                              let result7 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result7)
                        }
                        2 => {

                              let result10 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownRoot(result10)
                        }
                        3 => {

                              let result13 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::InvalidPath(result13)
                        }
                        4 => {

                              let result16 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotFound(result16)
                        }
                        5 => {

                              let result19 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::TooLarge(result19)
                        }
                        6 => {

                              let result22 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::Io(result22)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(root)
      ignore(path)
      @ffi.free(return_area)
      return lifted23

}

pub fn stat(root : String, path : String) -> Result[Entry, DriverError] {

      let return_area = @ffi.malloc(40)
      wasmImportStat(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(path), path.length(), return_area);

      let lifted28 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                  let lifted : UInt64? = match @ffi.load8_u((return_area) + 24) {
                        0 => Option::None
                        1 => {

                              Option::Some((@ffi.load64((return_area) + 32)).reinterpret_as_uint64())
                        }
                        _ => panic()
                  }

                  Result::Ok(Entry::{path : result, size : (@ffi.load64((return_area) + 16)).reinterpret_as_uint64(), modified : lifted})
            }
            1 => {

                  let lifted27 = match (@ffi.load8_u((return_area) + 8)) {
                        0 => {

                              let result8 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::PermissionDenied(result8)
                        }
                        1 => {

                              let result11 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::NotConfigured(result11)
                        }
                        2 => {

                              let result14 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::UnknownRoot(result14)
                        }
                        3 => {

                              let result17 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::InvalidPath(result17)
                        }
                        4 => {

                              let result20 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::NotFound(result20)
                        }
                        5 => {

                              let result23 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::TooLarge(result23)
                        }
                        6 => {

                              let result26 = @ffi.ptr2str(@ffi.load32((return_area) + 12), @ffi.load32((return_area) + 16))

                              DriverError::Io(result26)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted27)
            }
            _ => panic()
      }
      ignore(root)
      ignore(path)
      @ffi.free(return_area)
      return lifted28

}

pub fn list(root : String, prefix : String) -> Result[Array[Entry], DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportList(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(prefix), prefix.length(), return_area);

      let lifted30 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let array : Array[Entry] = [];
                  for index = 0; index < (@ffi.load32((return_area) + 8)); index = index + 1 {
                        let base1 = (@ffi.load32((return_area) + 4)) + (index * 32)

                        let result = @ffi.ptr2str(@ffi.load32((base1) + 0), @ffi.load32((base1) + 4))

                        let lifted : UInt64? = match @ffi.load8_u((base1) + 16) {
                              0 => Option::None
                              1 => {

                                    Option::Some((@ffi.load64((base1) + 24)).reinterpret_as_uint64())
                              }
                              _ => panic()
                        }

                        array.push(Entry::{path : result, size : (@ffi.load64((base1) + 8)).reinterpret_as_uint64(), modified : lifted})
                  }
                  @ffi.free(@ffi.load32((return_area) + 4))

                  Result::Ok(array)
            }
            1 => {

                  let lifted29 = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result10 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result10)
                        }
                        1 => {

                              let result13 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result13)
                        }
                        2 => {

                              let result16 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownRoot(result16)
                        }
                        3 => {

                              let result19 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::InvalidPath(result19)
                        }
                        4 => {

                              let result22 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotFound(result22)
                        }
                        5 => {

                              let result25 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::TooLarge(result25)
                        }
                        6 => {

                              let result28 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::Io(result28)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted29)
            }
            _ => panic()
      }
      ignore(root)
      ignore(prefix)
      @ffi.free(return_area)
      return lifted30

}

pub fn write(root : String, path : String, bytes : FixedArray[Byte]) -> Result[Unit, DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportWrite(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(path), path.length(), @ffi.bytes2ptr(bytes), bytes.length(), return_area);

      let lifted22 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result)
                        }
                        1 => {

                              let result6 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result6)
                        }
                        2 => {

                              let result9 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownRoot(result9)
                        }
                        3 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::InvalidPath(result12)
                        }
                        4 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotFound(result15)
                        }
                        5 => {

                              let result18 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::TooLarge(result18)
                        }
                        6 => {

                              let result21 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::Io(result21)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(root)
      ignore(path)
      ignore(bytes)
      @ffi.free(return_area)
      return lifted22

}

pub fn append(root : String, path : String, bytes : FixedArray[Byte]) -> Result[Unit, DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportAppend(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(path), path.length(), @ffi.bytes2ptr(bytes), bytes.length(), return_area);

      let lifted22 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result)
                        }
                        1 => {

                              let result6 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result6)
                        }
                        2 => {

                              let result9 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownRoot(result9)
                        }
                        3 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::InvalidPath(result12)
                        }
                        4 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotFound(result15)
                        }
                        5 => {

                              let result18 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::TooLarge(result18)
                        }
                        6 => {

                              let result21 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::Io(result21)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(root)
      ignore(path)
      ignore(bytes)
      @ffi.free(return_area)
      return lifted22

}

pub fn delete(root : String, path : String) -> Result[Unit, DriverError] {

      let return_area = @ffi.malloc(16)
      wasmImportDelete(@ffi.str2ptr(root), root.length(), @ffi.str2ptr(path), path.length(), return_area);

      let lifted22 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::PermissionDenied(result)
                        }
                        1 => {

                              let result6 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotConfigured(result6)
                        }
                        2 => {

                              let result9 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::UnknownRoot(result9)
                        }
                        3 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::InvalidPath(result12)
                        }
                        4 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::NotFound(result15)
                        }
                        5 => {

                              let result18 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::TooLarge(result18)
                        }
                        6 => {

                              let result21 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              DriverError::Io(result21)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(root)
      ignore(path)
      @ffi.free(return_area)
      return lifted22

}

