// Generated by `wit-bindgen` 0.45.1. DO NOT EDIT!

pub fn publish(driver : String, topic : String, payload : FixedArray[Byte]) -> Result[Unit, @busTypes.BusError] {

      let return_area = @ffi.malloc(16)
      wasmImportPublish(@ffi.str2ptr(driver), driver.length(), @ffi.str2ptr(topic), topic.length(), @ffi.bytes2ptr(payload), payload.length(), return_area);

      let lifted19 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  Result::Ok(())
            }
            1 => {

                  let lifted = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::PermissionDenied(result)
                        }
                        1 => {

                              let result6 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::UnknownDriver(result6)
                        }
                        2 => {

                              let result9 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::UnknownTopic(result9)
                        }
                        3 => {

                              let result12 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::DriverUnavailable(result12)
                        }
                        4 => {

                              let result15 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::QueueFull(result15)
                        }
                        5 => {

                              let result18 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::TooLarge(result18)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted)
            }
            _ => panic()
      }
      ignore(driver)
      ignore(topic)
      ignore(payload)
      @ffi.free(return_area)
      return lifted19

}

pub fn get(driver : String, topic : String) -> Result[FixedArray[Byte]?, @busTypes.BusError] {

      let return_area = @ffi.malloc(16)
      wasmImportGet(@ffi.str2ptr(driver), driver.length(), @ffi.str2ptr(topic), topic.length(), return_area);

      let lifted25 = match (@ffi.load8_u((return_area) + 0)) {
            0 => {

                  let lifted : FixedArray[Byte]? = match @ffi.load8_u((return_area) + 4) {
                        0 => Option::None
                        1 => {

                              let result = @ffi.ptr2bytes(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              Option::Some(result)
                        }
                        _ => panic()
                  }

                  Result::Ok(lifted)
            }
            1 => {

                  let lifted24 = match (@ffi.load8_u((return_area) + 4)) {
                        0 => {

                              let result8 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::PermissionDenied(result8)
                        }
                        1 => {

                              let result11 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::UnknownDriver(result11)
                        }
                        2 => {

                              let result14 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::UnknownTopic(result14)
                        }
                        3 => {

                              let result17 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::DriverUnavailable(result17)
                        }
                        4 => {

                              let result20 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::QueueFull(result20)
                        }
                        5 => {

                              let result23 = @ffi.ptr2str(@ffi.load32((return_area) + 8), @ffi.load32((return_area) + 12))

                              @busTypes.BusError::TooLarge(result23)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted24)
            }
            _ => panic()
      }
      ignore(driver)
      ignore(topic)
      @ffi.free(return_area)
      return lifted25

}

