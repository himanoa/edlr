# ゲスト SDK(Rust / Go)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** プラグイン作者が `core/wit` の cp・バインディング再生成なしに SDK 依存だけでプラグインを書けるようにし、submit/complete の job-id 突き合わせをコールバック式ヘルパーで肩代わりする。

**Architecture:** `sdk/rust`(crate `edlr-plugin-sdk`)は crate 内で `wit_bindgen::generate!` を 1 回だけ実行し、デフォルト実装付きの `Plugin` trait と `register!` マクロで bindgen の `Guest` を包む。`sdk/go`(module `github.com/himanoa/edlr/sdk/go`)は `wit-bindgen-go` の生成物と WIT のコピーを同梱し、`edlrplugin.Register(Hooks)` で `Exports` 配線を肩代わりする。両言語とも SDK 内部の pending マップで `on-job-complete` を demux し、未知の job-id は利用者のフックへ委譲する。既存 examples を全部 SDK 依存へ移行してテストフィクスチャ経由で SDK を CI に載せる。

**Tech Stack:** Rust(wit-bindgen 0.60、serde_json、base64 0.22)、Go/TinyGo(wit-bindgen-go v0.6.2、go.bytecodealliance.org/cm)、wasm32-wasip2。

## Global Constraints

- 設計 spec: `docs/superpowers/specs/2026-08-05-guest-sdk-design.md`(要一読)
- `core/wit/plugin.wit` は**変更しない**(現行 `edlr:plugin@0.5.0`)
- `.claude/rules/` の作法に従う(core 外でも新規コードは判断を純関数に切り出しテストする)
- sdk/rust・examples は workspace **非メンバー**のまま(ルート `Cargo.toml` の members に足さない)
- wit-bindgen(Rust)は `0.60.0`、wit-bindgen-go は `v0.6.2` で生成
- Rust SDK の crate バージョンは WIT に揃えて `0.5.0`
- フェーズゲート: 各タスク末尾で `cargo test --workspace` + `cargo clippy --workspace --all-targets` が全パス(examples/sdk 側のビルドはタスク内の個別コマンドで確認)
- サブエージェントを並列に走らせる場合は先に `cargo fetch` を 1 回実行(CLAUDE.md)

---

### Task 1: Rust SDK crate の骨格(bindings 内包 + Plugin trait + register!)

**Files:**
- Create: `sdk/rust/Cargo.toml`
- Create: `sdk/rust/src/lib.rs`
- Create: `sdk/rust/tests/smoke.rs`

**Interfaces:**
- Produces: crate `edlr-plugin-sdk`。`edlr_plugin_sdk::Plugin` trait(全メソッドデフォルト実装付き)、`edlr_plugin_sdk::register!` マクロ、`edlr_plugin_sdk::Event` 型、`edlr_plugin_sdk::bindings::edlr::plugin::{host_log, host_settings, driver_http, driver_process, driver_fs, bus}` の再エクスポート。Task 2 が `crate::http::dispatch_job_complete::<P>(job_id, result_json)` を差し込むフックとして、`register!` 内の `on_job_complete` shim は Task 1 時点では `P::on_job_complete` 直呼びにしておく(Task 2 で差し替え)

- [ ] **Step 1: Cargo.toml を書く**

```toml
[package]
name = "edlr-plugin-sdk"
version = "0.5.0"
edition = "2021"
description = "edlr プラグインを書くための SDK。WIT バインディングを内包する"
license = "MIT"

[dependencies]
wit-bindgen = "0.60.0"
serde_json = "1"
base64 = "0.22"

[lib]
crate-type = ["cdylib", "rlib"]
```

`rlib` はホスト(x86_64)での `cargo test` 用、`cdylib` は wasm ビルド用。

- [ ] **Step 2: src/lib.rs を書く**

```rust
//! edlr プラグイン SDK。
//!
//! WIT バインディングをこの crate が内包するので、プラグイン作者は
//! `core/wit` の cp も wit-bindgen の実行も不要。使い方は docs/sdk.md 参照。
//!
//! ```ignore
//! use edlr_plugin_sdk as sdk;
//! struct MyPlugin;
//! impl sdk::Plugin for MyPlugin {
//!     fn on_event(_ev: sdk::Event) {}
//! }
//! sdk::register!(MyPlugin);
//! ```

pub mod http;

#[doc(hidden)]
pub mod bindings {
    // Cargo の git 依存はリポジトリ全体を checkout するため、crate 外の
    // core/wit を相対パスで参照できる(spec「Rust SDK」参照)。
    // crates.io 公開時はこの前提が壊れるが、公開はスコープ外。
    wit_bindgen::generate!({
        path: "../../core/wit",
        world: "plugin",
        pub_export_macro: true,
    });
}

pub use bindings::edlr::plugin::{bus, driver_fs, driver_http, driver_process, host_log, host_settings};
pub use bindings::Event;

/// プラグイン本体が実装する trait。bindgen の `Guest`(全 export 必須)との
/// 緩衝層で、全メソッドにデフォルト実装があるため必要なものだけ書けばよい。
/// ABI に export が増えたときも、ここにデフォルト実装を足せば既存プラグインは
/// ソース互換のまま再ビルドだけで追従できる。
pub trait Plugin {
    fn init() {}
    fn on_event(_ev: Event) {}
    fn on_message(_driver: String, _topic: String, _payload: Vec<u8>) {}
    fn on_schedule(_name: String) {}
    /// SDK の `http::submit` を経由しないジョブ(自前で `submit-send` を
    /// 呼んだ場合)の完了だけがここへ届く(`register!` の demux 参照)。
    fn on_job_complete(_job_id: u64, _result_json: String) {}
    fn on_stop() {}
}

/// `Plugin` 実装を wasm の export として配線する。
#[macro_export]
macro_rules! register {
    ($plugin:ty) => {
        struct __EdlrSdkShim;
        impl $crate::bindings::Guest for __EdlrSdkShim {
            fn init() {
                <$plugin as $crate::Plugin>::init()
            }
            fn on_event(ev: $crate::Event) {
                <$plugin as $crate::Plugin>::on_event(ev)
            }
            fn on_message(driver: String, topic: String, payload: Vec<u8>) {
                <$plugin as $crate::Plugin>::on_message(driver, topic, payload)
            }
            fn on_schedule(name: String) {
                <$plugin as $crate::Plugin>::on_schedule(name)
            }
            fn on_job_complete(job_id: u64, result_json: String) {
                // Task 2 で $crate::http::dispatch_job_complete::<$plugin> に差し替える
                <$plugin as $crate::Plugin>::on_job_complete(job_id, result_json)
            }
            fn on_stop() {
                <$plugin as $crate::Plugin>::on_stop()
            }
        }
        $crate::bindings::export!(__EdlrSdkShim with_types_in $crate::bindings);
    };
}
```

`src/http.rs` は Task 1 時点では空に近い形で置く(`pub` アイテムなし、`//! Task 2 で実装` のみ)。

- [ ] **Step 3: smoke テストを書く**

`sdk/rust/tests/smoke.rs`(ホストターゲットでコンパイルが通ること・trait のデフォルト実装が効くことの確認。wasm 実行はしない):

```rust
//! SDK がホストターゲットでもコンパイルでき、`Plugin` のデフォルト実装で
//! 空実装のプラグインが書けることの確認。`register!` は wasm ターゲット
//! 専用(export シンボルを生やす)なのでここでは呼ばない。

use edlr_plugin_sdk as sdk;

struct Empty;
impl sdk::Plugin for Empty {}

#[test]
fn default_impls_cover_every_export() {
    // 全メソッドがデフォルト実装で呼べる(パニックしない)こと。
    <Empty as sdk::Plugin>::init();
    <Empty as sdk::Plugin>::on_schedule("flush".to_string());
    <Empty as sdk::Plugin>::on_job_complete(1, "{}".to_string());
    <Empty as sdk::Plugin>::on_stop();
}
```

- [ ] **Step 4: テストを実行して通す**

Run: `cd sdk/rust && cargo test`
Expected: PASS(コンパイルエラーが出たら generate! のオプション名・re-export パスを `examples/plugins/http-caller` の生成物を参考に修正)

- [ ] **Step 5: wasm ターゲットでもビルドできることを確認**

Run: `cd sdk/rust && cargo build --target wasm32-wasip2 --release`
Expected: 成功

- [ ] **Step 6: Commit**

```bash
git add sdk/rust
git commit -m "feat(sdk): Rust プラグイン SDK の骨格(bindings 内包 + Plugin trait + register!)"
```

---

### Task 2: Rust SDK の http::submit ヘルパー(pending demux + result-json デコード)

**Files:**
- Modify: `sdk/rust/src/http.rs`
- Modify: `sdk/rust/src/lib.rs`(`register!` の `on_job_complete` shim を差し替え)

**Interfaces:**
- Consumes: `bindings::edlr::plugin::driver_http::{submit_send, Request, DriverError}`(Task 1)
- Produces: `http::Response { status: u16, headers: Vec<(String, String)>, body: Vec<u8> }`、`http::JobError`(enum `Transport(String)` / `InvalidRequest(String)` / `Malformed(String)`)、`http::submit(request: Request, timeout_ms: Option<u32>, callback: impl FnOnce(Result<Response, JobError>) + 'static) -> Result<u64, DriverError>`、`http::dispatch_job_complete::<P: Plugin>(job_id: u64, result_json: String)`(register! 専用、`#[doc(hidden)]`)。Task 3 以降の examples はこの `submit` を使う

- [ ] **Step 1: 純関数 parse_job_result の失敗するテストを書く**

`src/http.rs` の `#[cfg(test)] mod tests` に(result-json の形は `docs/plugins.md`「非同期 HTTP」参照):

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn ok_result_decodes_status_headers_and_base64_body() {
        let json = r#"{"ok":{"status":200,"headers":[["x-a","b"]],"body-base64":"aGVsbG8="}}"#;
        let response = parse_job_result(json).expect("ok result should parse");
        assert_eq!(response.status, 200);
        assert_eq!(response.headers, vec![("x-a".to_string(), "b".to_string())]);
        assert_eq!(response.body, b"hello");
    }

    #[test]
    fn err_result_maps_kind_to_job_error() {
        let json = r#"{"err":{"kind":"transport","message":"boom"}}"#;
        assert!(matches!(
            parse_job_result(json),
            Err(JobError::Transport(m)) if m == "boom"
        ));
        let json = r#"{"err":{"kind":"invalid-request","message":"bad"}}"#;
        assert!(matches!(
            parse_job_result(json),
            Err(JobError::InvalidRequest(m)) if m == "bad"
        ));
    }

    #[test]
    fn garbage_json_is_malformed_not_a_panic() {
        assert!(matches!(parse_job_result("not json"), Err(JobError::Malformed(_))));
        assert!(matches!(parse_job_result("{}"), Err(JobError::Malformed(_))));
        // ok はあるが base64 が壊れている
        let json = r#"{"ok":{"status":200,"headers":[],"body-base64":"%%%"}}"#;
        assert!(matches!(parse_job_result(json), Err(JobError::Malformed(_))));
    }

    #[test]
    fn pending_map_resolves_once_and_delegates_unknown_ids() {
        use std::cell::Cell;
        let resolved = std::rc::Rc::new(Cell::new(0u32));
        {
            let resolved = resolved.clone();
            register_pending(7, Box::new(move |_| resolved.set(resolved.get() + 1)));
        }
        // 登録済み id は解決され、二度目は「未知」になる。
        assert!(take_pending(7).is_some());
        assert!(take_pending(7).is_none());
        assert!(take_pending(999).is_none());
    }
}
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `cd sdk/rust && cargo test`
Expected: FAIL(`parse_job_result` 未定義のコンパイルエラー)

- [ ] **Step 3: http.rs を実装する**

```rust
//! `driver-http.submit-send` のコールバック式ヘルパー
//! (issue sdk-send-async-response-await-lvn3)。
//!
//! job-id → コールバックの pending マップを SDK が持ち、`on-job-complete`
//! の配送(`register!` の shim → `dispatch_job_complete`)で解決する。
//! demux は job-id だけを見る(job 種別に依存しない)ので、HTTP 以外の
//! job が将来増えても構造は変わらない。SDK を経由せず自前で `submit-send`
//! を呼んだ job の完了は、pending に無い id として `Plugin::on_job_complete`
//! へ委譲される。

use std::cell::RefCell;
use std::collections::HashMap;

use base64::Engine as _;

use crate::bindings::edlr::plugin::driver_http::{submit_send, DriverError, Request};
use crate::Plugin;

/// デコード済みの submit 結果。`body` は base64 を復元したバイト列。
#[derive(Debug, PartialEq)]
pub struct Response {
    pub status: u16,
    pub headers: Vec<(String, String)>,
    pub body: Vec<u8>,
}

/// submit したジョブが失敗したときの結果。
/// `Malformed` はホストの `result-json` が想定形でなかった場合
/// (通常起こらない。SDK と core のバージョン不一致など)。
#[derive(Debug, PartialEq)]
pub enum JobError {
    Transport(String),
    InvalidRequest(String),
    Malformed(String),
}

type Callback = Box<dyn FnOnce(Result<Response, JobError>)>;

thread_local! {
    // wasm ゲストはシングルスレッドなので thread_local + RefCell で足りる。
    static PENDING: RefCell<HashMap<u64, Callback>> = RefCell::new(HashMap::new());
}

/// リクエストを非同期に投げ、完了時に `callback` を(次以降のいずれかの
/// export 呼び出しの中で)起動する。受付が拒否された場合(未承認 /
/// in-flight 上限)はコールバックを登録せず同期の `Err` を返す。
pub fn submit(
    request: Request,
    timeout_ms: Option<u32>,
    callback: impl FnOnce(Result<Response, JobError>) + 'static,
) -> Result<u64, DriverError> {
    let job_id = submit_send(&request, timeout_ms)?;
    register_pending(job_id, Box::new(callback));
    Ok(job_id)
}

fn register_pending(job_id: u64, callback: Callback) {
    PENDING.with(|pending| pending.borrow_mut().insert(job_id, callback));
}

fn take_pending(job_id: u64) -> Option<Callback> {
    PENDING.with(|pending| pending.borrow_mut().remove(job_id))
}

/// `register!` の `on-job-complete` shim 専用。SDK 利用者は呼ばない。
#[doc(hidden)]
pub fn dispatch_job_complete<P: Plugin>(job_id: u64, result_json: String) {
    match take_pending(job_id) {
        Some(callback) => callback(parse_job_result(&result_json)),
        None => P::on_job_complete(job_id, result_json),
    }
}

/// `result-json`(docs/plugins.md「非同期 HTTP」の形)を値へ変換する純関数。
fn parse_job_result(result_json: &str) -> Result<Response, JobError> {
    let value: serde_json::Value = serde_json::from_str(result_json)
        .map_err(|e| JobError::Malformed(format!("result-json is not JSON: {e}")))?;
    if let Some(err) = value.get("err") {
        let message = err["message"].as_str().unwrap_or_default().to_string();
        return Err(match err["kind"].as_str() {
            Some("transport") => JobError::Transport(message),
            Some("invalid-request") => JobError::InvalidRequest(message),
            other => JobError::Malformed(format!("unknown err kind: {other:?}")),
        });
    }
    let Some(ok) = value.get("ok") else {
        return Err(JobError::Malformed("neither ok nor err".to_string()));
    };
    let status = ok["status"]
        .as_u64()
        .and_then(|s| u16::try_from(s).ok())
        .ok_or_else(|| JobError::Malformed("missing/invalid status".to_string()))?;
    let headers = ok["headers"]
        .as_array()
        .map(|hs| {
            hs.iter()
                .filter_map(|h| {
                    Some((h[0].as_str()?.to_string(), h[1].as_str()?.to_string()))
                })
                .collect()
        })
        .unwrap_or_default();
    let body = base64::engine::general_purpose::STANDARD
        .decode(ok["body-base64"].as_str().unwrap_or_default())
        .map_err(|e| JobError::Malformed(format!("invalid body-base64: {e}")))?;
    Ok(Response {
        status,
        headers,
        body,
    })
}
```

`take_pending` の引数は `&u64` でなく `u64`(`remove` へは `&job_id` を渡す)。コンパイルエラーが出たら合わせる。

- [ ] **Step 4: lib.rs の register! の shim を差し替える**

```rust
            fn on_job_complete(job_id: u64, result_json: String) {
                $crate::http::dispatch_job_complete::<$plugin>(job_id, result_json)
            }
```

- [ ] **Step 5: テストを実行して通す**

Run: `cd sdk/rust && cargo test && cargo build --target wasm32-wasip2 --release`
Expected: 両方 PASS

- [ ] **Step 6: Commit**

```bash
git add sdk/rust
git commit -m "feat(sdk): http::submit コールバックヘルパーと on-job-complete demux"
```

---

### Task 3: hello-logger を SDK へ移行(移行パターンの確立)

**Files:**
- Modify: `examples/plugins/hello-logger/Cargo.toml`
- Modify: `examples/plugins/hello-logger/src/lib.rs`

**Interfaces:**
- Consumes: `edlr_plugin_sdk::{Plugin, Event, host_log, host_settings, register!}`(Task 1)
- Produces: 以降の Rust example 移行(Task 4)がコピーする形。依存の書き方は `edlr-plugin-sdk = { path = "../../../sdk/rust" }`

- [ ] **Step 1: Cargo.toml の依存を差し替える**

`wit-bindgen` を消し、`edlr-plugin-sdk = { path = "../../../sdk/rust" }` を足す(serde_json 等、プラグイン自身が使う依存は残す)。

- [ ] **Step 2: src/lib.rs を書き換える**

`wit_bindgen::generate!` ブロックと `use` 群を消し、SDK 経由へ:

```rust
use edlr_plugin_sdk as sdk;
use sdk::host_log::{log, Level};
```

- `impl Guest for HelloLogger` → `impl sdk::Plugin for HelloLogger`
- 空実装のメソッド(`on_message` / `on_job_complete` / `on_schedule` / `on_stop`)は**削除**(デフォルト実装に任せる — これが SDK の売り)
- `export!(HelloLogger);` → `sdk::register!(HelloLogger);`
- `Event` 型は `sdk::Event`

- [ ] **Step 3: wasm をビルドして export を検証する**

Run:
```bash
cd examples/plugins/hello-logger && cargo build --target wasm32-wasip2 --release
wasm-tools component wit target/wasm32-wasip2/release/hello_logger.wasm | grep -E 'export|@0.5.0' | head
```
Expected: `init` / `on-event` / `on-message` / `on-schedule` / `on-job-complete` / `on-stop` の 6 export と `edlr:plugin@0.5.0`

- [ ] **Step 4: 統合テストで実際にロードされることを確認**

Run: `cargo test -p edlr-core --test plugin_host_integration 2>&1 | tail -5`
Expected: 全 PASS(このテストは hello-logger をビルドしてロード・init まで行う)

- [ ] **Step 5: Commit**

```bash
git add examples/plugins/hello-logger
git commit -m "refactor(examples): hello-logger を edlr-plugin-sdk へ移行"
```

---

### Task 4: 残りの Rust examples 6 個を SDK へ移行

**Files:**
- Modify: `examples/plugins/{busy-loop,init-trap,memory-hog,state-reader,http-caller,tutorial-jump-log-rs}/Cargo.toml`
- Modify: 同各 `src/lib.rs`

**Interfaces:**
- Consumes: Task 3 と同じ移行パターン + `sdk::http::submit`(Task 2)、`sdk::{driver_http, bus, host_settings}` 再エクスポート(Task 1)

- [ ] **Step 1: busy-loop / init-trap / memory-hog / state-reader を移行**

Task 3 のパターンをそのまま適用(依存差し替え・`Plugin` 化・空メソッド削除・`register!`)。state-reader は `bus` を使うので `use sdk::bus;` に読み替える。

- [ ] **Step 2: http-caller を移行し、submit 例を sdk::http::submit へ書き換える**

同期 `send` の例は `sdk::driver_http::{send, DriverError, Request}` で現状維持。手書きの `submit_send` + `on_job_complete` 実装を削除し、`on_event` 内を:

```rust
        match sdk::http::submit(request, None, move |result| match result {
            Ok(response) => log(
                Level::Info,
                &format!("http-caller: job completed -> status {}", response.status),
            ),
            Err(e) => log(Level::Warn, &format!("http-caller: job failed: {e:?}")),
        }) {
            Ok(job_id) => log(Level::Info, &format!("http-caller: submitted as job {job_id}")),
            Err(e) => log(Level::Warn, &format!("http-caller: submit failed: {e:?}")),
        }
```

`fn on_job_complete` は削除(SDK の demux が受ける)。

- [ ] **Step 3: tutorial-jump-log-rs を移行**

同パターン。`jumplog` モジュール(純関数群)は無変更。`driver_http::send` / `bus::publish` / `bus::get` / `host_settings::get_all` は `sdk::` 再エクスポートへ読み替え。

- [ ] **Step 4: 6 個全部を wasm ビルドして確認**

Run:
```bash
for p in busy-loop init-trap memory-hog state-reader http-caller tutorial-jump-log-rs; do
  (cd examples/plugins/$p && cargo build --target wasm32-wasip2 --release) || echo "FAIL: $p"
done
```
Expected: FAIL 出力なし

- [ ] **Step 5: フルゲート**

Run: `cargo test --workspace 2>&1 | grep -E 'FAILED|[1-9][0-9]* failed'; cargo clippy --workspace --all-targets 2>&1 | grep -cE '^(warning|error)'`
Expected: 空出力 + `0`(state-reader は bus_integration が、http-caller は plugin_host_integration が実ロードする)

- [ ] **Step 6: Commit**

```bash
git add examples/plugins
git commit -m "refactor(examples): 残りの Rust ゲスト 6 個を edlr-plugin-sdk へ移行"
```

---

### Task 5: Go SDK の WIT 同梱と core 側の同期テスト

**Files:**
- Create: `sdk/go/wit/`(`core/wit` のコピー)
- Create: `core/tests/sdk_wit_sync.rs`

**Interfaces:**
- Produces: `sdk/go/wit/`(Task 6 の生成・Task 7 のビルドが参照)。core テスト `sdk_wit_sync` が `core/wit` と `sdk/go/wit` のファイル名・内容の完全一致を保証

- [ ] **Step 1: 失敗するテストを書く**

`core/tests/sdk_wit_sync.rs`:

```rust
//! `sdk/go/wit/` は `core/wit/` のコピー(Go module の zip はモジュール
//! ディレクトリしか含まないため、tinygo build 用に WIT を SDK へ同梱して
//! いる。docs/superpowers/specs/2026-08-05-guest-sdk-design.md 参照)。
//! ABI 変更時の cp 忘れをここで機械的に検出する。ずれていたら
//! `cp -r core/wit/. sdk/go/wit/` で追従すること。

use std::collections::BTreeMap;
use std::path::Path;

fn wit_files(dir: &Path) -> BTreeMap<String, String> {
    std::fs::read_dir(dir)
        .unwrap_or_else(|e| panic!("read {}: {e}", dir.display()))
        .filter_map(Result::ok)
        .filter(|entry| entry.path().extension().is_some_and(|ext| ext == "wit"))
        .map(|entry| {
            let name = entry.file_name().to_string_lossy().into_owned();
            let body = std::fs::read_to_string(entry.path()).expect("read wit file");
            (name, body)
        })
        .collect()
}

#[test]
fn sdk_go_wit_matches_core_wit() {
    let root = Path::new(env!("CARGO_MANIFEST_DIR")).join("..");
    let core = wit_files(&root.join("core/wit"));
    let sdk = wit_files(&root.join("sdk/go/wit"));
    assert_eq!(
        core, sdk,
        "sdk/go/wit must be an exact copy of core/wit; run: cp -r core/wit/. sdk/go/wit/"
    );
}
```

注: `core/wit` に `deps/` などのサブディレクトリがある場合はテストを再帰に拡張する(`ls core/wit` を見て判断)。

- [ ] **Step 2: テストが落ちることを確認**

Run: `cargo test -p edlr-core --test sdk_wit_sync`
Expected: FAIL(`sdk/go/wit` がまだ無い)

- [ ] **Step 3: WIT をコピーする**

Run: `mkdir -p sdk/go/wit && cp -r core/wit/. sdk/go/wit/`

- [ ] **Step 4: テストを実行して通す**

Run: `cargo test -p edlr-core --test sdk_wit_sync`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add sdk/go/wit core/tests/sdk_wit_sync.rs
git commit -m "feat(sdk): Go SDK へ WIT を同梱し core/wit との同期テストを追加"
```

---

### Task 6: Go SDK 本体(gen 同梱 + edlrplugin パッケージ)

**Files:**
- Create: `sdk/go/go.mod`(module `github.com/himanoa/edlr/sdk/go`)
- Create: `sdk/go/gen/`(wit-bindgen-go 生成物)
- Create: `sdk/go/edlrplugin/edlrplugin.go`
- Create: `sdk/go/edlrplugin/jobs.go`
- Create: `sdk/go/edlrplugin/jobs_test.go`
- Create: `sdk/go/README.md`

**Interfaces:**
- Consumes: `sdk/go/wit`(Task 5)
- Produces: `edlrplugin.Hooks`(フィールド: `Init func()` / `OnEvent func(Event)` / `OnMessage func(driver, topic string, payload []byte)` / `OnSchedule func(name string)` / `OnJobComplete func(jobID uint64, resultJSON string)` / `OnStop func()`)、`edlrplugin.Register(Hooks)`、`edlrplugin.SubmitHTTP(req Request, timeoutMS *uint32, cb func(*Response, error)) (uint64, error)`、`edlrplugin.Response { Status uint16; Headers [][2]string; Body []byte }`。型エイリアス `Event` / `Request` は gen の driver-http / plugin 型を指す。Task 7 の Go examples がこれを使う

- [ ] **Step 1: go.mod と gen を作る**

```bash
cd sdk/go
go mod init github.com/himanoa/edlr/sdk/go
wit-bindgen-go generate --world plugin --out gen ./wit
go mod tidy
```

`go.bytecodealliance.org/cm` のバージョンは `examples/plugins/inara-uploader/go.mod` と揃える。

- [ ] **Step 2: 純関数(result-json デコード + pending demux)の失敗するテストを書く**

`sdk/go/edlrplugin/jobs_test.go`:

```go
package edlrplugin

import (
	"reflect"
	"testing"
)

func TestParseJobResultOK(t *testing.T) {
	json := `{"ok":{"status":200,"headers":[["x-a","b"]],"body-base64":"aGVsbG8="}}`
	resp, err := parseJobResult(json)
	if err != nil {
		t.Fatalf("expected ok, got %v", err)
	}
	if resp.Status != 200 || !reflect.DeepEqual(resp.Body, []byte("hello")) {
		t.Fatalf("bad decode: %+v", resp)
	}
	if len(resp.Headers) != 1 || resp.Headers[0] != [2]string{"x-a", "b"} {
		t.Fatalf("bad headers: %+v", resp.Headers)
	}
}

func TestParseJobResultErrKinds(t *testing.T) {
	for _, tc := range []struct{ json, want string }{
		{`{"err":{"kind":"transport","message":"boom"}}`, "transport: boom"},
		{`{"err":{"kind":"invalid-request","message":"bad"}}`, "invalid-request: bad"},
	} {
		if _, err := parseJobResult(tc.json); err == nil || err.Error() != tc.want {
			t.Fatalf("json %s: got %v, want %s", tc.json, err, tc.want)
		}
	}
}

func TestParseJobResultMalformed(t *testing.T) {
	for _, json := range []string{"not json", "{}", `{"ok":{"status":200,"headers":[],"body-base64":"%%%"}}`} {
		if _, err := parseJobResult(json); err == nil {
			t.Fatalf("json %s: expected an error", json)
		}
	}
}

func TestPendingResolvesOnceAndUnknownDelegates(t *testing.T) {
	resolved := 0
	registerPending(7, func(*Response, error) { resolved++ })
	if cb := takePending(7); cb == nil {
		t.Fatal("registered id must resolve")
	} else {
		cb(nil, nil)
	}
	if takePending(7) != nil {
		t.Fatal("second take must be nil (resolve exactly once)")
	}
	if takePending(999) != nil {
		t.Fatal("unknown id must be nil")
	}
	if resolved != 1 {
		t.Fatalf("callback ran %d times", resolved)
	}
}
```

- [ ] **Step 3: テストが落ちることを確認**

Run: `cd sdk/go && go test ./edlrplugin/`
Expected: FAIL(未定義エラー)

- [ ] **Step 4: jobs.go を実装する**

```go
package edlrplugin

import (
	"encoding/base64"
	"encoding/json"
	"fmt"
)

// Response は submit したジョブのデコード済み結果。Body は base64 を
// 復元したバイト列。
type Response struct {
	Status  uint16
	Headers [][2]string
	Body    []byte
}

type jobCallback func(*Response, error)

// wasm ゲストはシングルスレッドなので素の map でよい。
var pending = map[uint64]jobCallback{}

func registerPending(jobID uint64, cb jobCallback) { pending[jobID] = cb }

func takePending(jobID uint64) jobCallback {
	cb, ok := pending[jobID]
	if !ok {
		return nil
	}
	delete(pending, jobID)
	return cb
}

type jobResultJSON struct {
	OK *struct {
		Status     uint16      `json:"status"`
		Headers    [][2]string `json:"headers"`
		BodyBase64 string      `json:"body-base64"`
	} `json:"ok"`
	Err *struct {
		Kind    string `json:"kind"`
		Message string `json:"message"`
	} `json:"err"`
}

// parseJobResult は result-json(docs/plugins.md「非同期 HTTP」の形)を
// 値へ変換する純関数。
func parseJobResult(resultJSON string) (*Response, error) {
	var v jobResultJSON
	if err := json.Unmarshal([]byte(resultJSON), &v); err != nil {
		return nil, fmt.Errorf("result-json is not JSON: %w", err)
	}
	if v.Err != nil {
		return nil, fmt.Errorf("%s: %s", v.Err.Kind, v.Err.Message)
	}
	if v.OK == nil {
		return nil, fmt.Errorf("malformed result-json: neither ok nor err")
	}
	body, err := base64.StdEncoding.DecodeString(v.OK.BodyBase64)
	if err != nil {
		return nil, fmt.Errorf("invalid body-base64: %w", err)
	}
	return &Response{Status: v.OK.Status, Headers: v.OK.Headers, Body: body}, nil
}

// dispatchJobComplete は Register が Exports.OnJobComplete へ配線する実体。
// SDK 経由(SubmitHTTP)の job は pending から解決し、未知の id は
// Hooks.OnJobComplete へ委譲する。
func dispatchJobComplete(hooks Hooks) func(jobID uint64, resultJSON string) {
	return func(jobID uint64, resultJSON string) {
		if cb := takePending(jobID); cb != nil {
			cb(parseJobResult(resultJSON))
			return
		}
		if hooks.OnJobComplete != nil {
			hooks.OnJobComplete(jobID, resultJSON)
		}
	}
}
```

- [ ] **Step 5: edlrplugin.go(Register + SubmitHTTP)を実装する**

```go
// Package edlrplugin は edlr プラグインを Go で書くための SDK。
// WIT バインディング(gen/)と WIT 本体(wit/)を同梱しているので、
// プラグイン作者は core/wit の cp も wit-bindgen-go の実行も不要。
// 使い方はリポジトリの docs/sdk.md を参照。
package edlrplugin

import (
	"go.bytecodealliance.org/cm"

	driverhttp "github.com/himanoa/edlr/sdk/go/gen/edlr/plugin/driver-http"
	"github.com/himanoa/edlr/sdk/go/gen/edlr/plugin/plugin"
)

// Event / Request は gen の型をそのまま使う(再エクスポート)。
type (
	Event   = plugin.Event
	Request = driverhttp.Request
)

// Hooks は export の実装。未設定(nil)のフックは no-op になる。
// OnJobComplete には SubmitHTTP を経由しない job の完了だけが届く。
type Hooks struct {
	Init          func()
	OnEvent       func(Event)
	OnMessage     func(driver, topic string, payload []byte)
	OnSchedule    func(name string)
	OnJobComplete func(jobID uint64, resultJSON string)
	OnStop        func()
}

// Register は Hooks を wasm の export として配線する。init() から呼ぶこと。
func Register(hooks Hooks) {
	plugin.Exports.Init = func() {
		if hooks.Init != nil {
			hooks.Init()
		}
	}
	plugin.Exports.OnEvent = func(ev Event) {
		if hooks.OnEvent != nil {
			hooks.OnEvent(ev)
		}
	}
	plugin.Exports.OnMessage = func(driver, topic string, payload cm.List[uint8]) {
		if hooks.OnMessage != nil {
			hooks.OnMessage(driver, topic, payload.Slice())
		}
	}
	plugin.Exports.OnSchedule = func(name string) {
		if hooks.OnSchedule != nil {
			hooks.OnSchedule(name)
		}
	}
	plugin.Exports.OnJobComplete = dispatchJobComplete(hooks)
	plugin.Exports.OnStop = func() {
		if hooks.OnStop != nil {
			hooks.OnStop()
		}
	}
}

// SubmitHTTP はリクエストを非同期に投げ、完了時に cb を起動する。
// 受付が拒否された場合(未承認 / in-flight 上限)は cb を登録せず
// 同期の error を返す。
func SubmitHTTP(req Request, timeoutMS *uint32, cb func(*Response, error)) (uint64, error) {
	timeout := cm.None[uint32]()
	if timeoutMS != nil {
		timeout = cm.Some(*timeoutMS)
	}
	result := driverhttp.SubmitSend(req, timeout)
	if result.IsErr() {
		return 0, fmt.Errorf("submit-send: %v", result.Err())
	}
	jobID := *result.OK()
	registerPending(jobID, cb)
	return jobID, nil
}
```

`cm.Option` / `cm.Result` の正確な API は gen の `driver-http` パッケージと `examples/plugins/inara-uploader` の既存呼び出しを見て合わせる(名前が違えばそちらが正)。

- [ ] **Step 6: テストを実行して通す**

Run: `cd sdk/go && go vet ./... && go test ./edlrplugin/`
Expected: PASS

- [ ] **Step 7: README.md を書く**

`sdk/go/README.md` に最低限: module パス、`go get github.com/himanoa/edlr/sdk/go@sdk/go/v0.5.0`、tinygo build の `--wit-package "$(go list -m -f '{{.Dir}}' github.com/himanoa/edlr/sdk/go)/wit" --wit-world plugin-guest`、詳細は `docs/sdk.md` 参照の一文。

- [ ] **Step 8: Commit**

```bash
git add sdk/go
git commit -m "feat(sdk): Go SDK(gen 同梱 + edlrplugin の Register / SubmitHTTP)"
```

---

### Task 7: Go examples 2 個を SDK へ移行

**Files:**
- Modify: `examples/plugins/inara-uploader/{go.mod,main.go,build.sh}`
- Delete: `examples/plugins/inara-uploader/gen/`
- Modify: `examples/plugins/tutorial-jump-log-go/{go.mod,main.go,build.sh}`
- Delete: `examples/plugins/tutorial-jump-log-go/gen/`

**Interfaces:**
- Consumes: `edlrplugin.{Register, Hooks, Event, SubmitHTTP}`(Task 6)。ローカル参照は `replace github.com/himanoa/edlr/sdk/go => ../../../sdk/go`

- [ ] **Step 1: inara-uploader を移行する**

- `go.mod`: `require github.com/himanoa/edlr/sdk/go v0.0.0` + `replace github.com/himanoa/edlr/sdk/go => ../../../sdk/go`、`go mod tidy`
- `main.go`: `gen/...` の import を `sdk/go/gen/...` または `edlrplugin` へ差し替え。`init()` の `plugin.Exports.*` 代入を `edlrplugin.Register(edlrplugin.Hooks{ Init: onInit, OnEvent: onEvent, OnSchedule: onSchedule, OnStop: onStop })` に(`OnJobComplete` は未設定 = no-op)
- `uploader` / `inara` 等の内部パッケージが gen の型(hostlog / driverhttp など)を import している場合は `sdk/go/gen/...` パスへ機械置換
- `gen/` ディレクトリを削除
- `build.sh`: `--wit-package` を `../../../sdk/go/wit` へ(リポジトリ内なので相対でよい。外部利用者向けの `go list -m` 形は docs/sdk.md 側に書く)

- [ ] **Step 2: ビルドと Go テスト**

Run: `cd examples/plugins/inara-uploader && go test ./... && ./build.sh`
Expected: テスト全 PASS + `built: .../plugin.wasm`

- [ ] **Step 3: tutorial-jump-log-go を同様に移行する**

`OnMessage` を使っているので `Hooks.OnMessage` も設定する。`gen/` 削除、build.sh の `--wit-package` 差し替えも同様。

Run: `cd examples/plugins/tutorial-jump-log-go && ./build.sh`
Expected: `built: .../plugin.wasm`

- [ ] **Step 4: wasm の中身を確認**

Run: `wasm-tools component wit examples/plugins/inara-uploader/plugin.wasm | grep -cE 'on-job-complete|@0.5.0'`
Expected: `2`

- [ ] **Step 5: フルゲート**

Run: `cargo test --workspace 2>&1 | grep -E 'FAILED|[1-9][0-9]* failed'; cargo clippy --workspace --all-targets 2>&1 | grep -cE '^(warning|error)'`
Expected: 空出力 + `0`

- [ ] **Step 6: Commit**

```bash
git add -A examples/plugins/inara-uploader examples/plugins/tutorial-jump-log-go
git commit -m "refactor(examples): Go ゲスト 2 個を sdk/go(edlrplugin)へ移行"
```

---

### Task 8: docs / issue 更新

**Files:**
- Create: `docs/sdk.md`
- Modify: `docs/plugins.md`(再ビルド節)
- Modify: `docs/plugin-tutorial-rust.md` / `docs/plugin-tutorial-tinygo.md`(冒頭に誘導 1 行)
- issue: `golang-rust-sdk-9h8l` / `sdk-send-async-response-await-lvn3` を close

**Interfaces:**
- Consumes: Task 1-7 の成果すべて(コード例は実物からコピーする)

- [ ] **Step 1: docs/sdk.md を書く**

構成(コード例は移行済みの hello-logger / http-caller / tutorial-jump-log-go から実物をコピーする):

1. なにが嬉しいか(cp・再生成が不要。ABI バンプは tag を上げて再ビルドするだけ)
2. Rust: 依存の書き方(`git = "https://github.com/himanoa/edlr", tag = "sdk/v0.5.0"`)、`Plugin` trait(デフォルト実装で必要なものだけ書く)、`register!`、`http::submit` のコールバック式の説明(結果はどこかの export 呼び出しの中で届く・未知 job-id は `on_job_complete` へ委譲)
3. Go: `go get github.com/himanoa/edlr/sdk/go@sdk/go/v0.5.0`、`Hooks`/`Register`、`SubmitHTTP`、tinygo build の `--wit-package "$(go list -m -f '{{.Dir}}' github.com/himanoa/edlr/sdk/go)/wit" --wit-world plugin-guest`
4. バージョニング(SDK バージョン = WIT バージョン。tag は `sdk/v<wit>` / `sdk/go/v<wit>`)
5. 低レベル経路(生 wit-bindgen)を使いたい場合はチュートリアル参照

- [ ] **Step 2: 既存 docs へ誘導を足す**

- `docs/plugins.md` の「プラグインを新しい `core/wit` に対して再ビルドすること」の段落に「SDK(docs/sdk.md)経由なら依存の tag を上げて再ビルドするだけでよい」を追記
- `docs/plugin-tutorial-rust.md` / `docs/plugin-tutorial-tinygo.md` の冒頭に「このチュートリアルは低レベル経路(生 wit-bindgen)の解説。手早く書くなら SDK(docs/sdk.md)を使う」の 1 行(MoonBit は SDK 対象外なので触らない)

- [ ] **Step 3: issue を close する**

```bash
WT=$(git issues path)
# golang-rust-sdk-9h8l: 実装内容(sdk/rust・sdk/go の構成、配布形態、examples 全移行)を本文へ追記し summary を更新
# sdk-send-async-response-await-lvn3: コールバック式で実装した旨(future ではない理由: ゲストはシングルスレッド同期)と実装場所を追記し summary を更新
GIT_EDITOR=true git issues edit golang-rust-sdk-9h8l
git issues close golang-rust-sdk-9h8l
GIT_EDITOR=true git issues edit sdk-send-async-response-await-lvn3
git issues close sdk-send-async-response-await-lvn3
```

- [ ] **Step 4: 最終ゲート**

Run: `cargo test --workspace 2>&1 | grep -cE '0 failed'; cargo clippy --workspace --all-targets 2>&1 | grep -cE '^(warning|error)'; (cd sdk/rust && cargo test -q); (cd sdk/go && go test ./edlrplugin/)`
Expected: すべて成功

- [ ] **Step 5: Commit**

```bash
git add docs
git commit -m "docs: ゲスト SDK の使い方(docs/sdk.md)と既存ドキュメントからの誘導"
```
