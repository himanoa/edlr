# EliteDangerousLogRouter (edlr) 仕様書

会話ログ(2026-07 の設計議論)で最終的に確定した仕様のまとめ。

## 名称

- **正式名称**: EliteDangerousLogRouter
- **略称・実行コマンド名・パッケージ名**: `edlr`
- 命名方針: 名前は入口の事実(何をするものか)に留め、アーキテクチャの説明は README に委ねる。Journal を読むのは最初の一歩にすぎず、本体はイベントをドライバとプラグインへ配ること(= Router)である。
  - 途中案 `EliteDangerousLogReader` は却下され、Router で確定。

## 目的・ユースケース

Elite Dangerous のプレイ支援レイヤー(EDMC 相当)の自作。EDMC の不満点である「プラグイン・UI・監視が一体化した密結合」を避けることが設計の中心動機。

- Journal ファイルの tail(追記の検知と読み取り)
- JSON Lines のパース
- `Status.json` の変更監視
- イベントの配信(音声通知・翻訳パイプラインなどの下流コンポーネントへの供給)

ワークロードは完全に I/O バウンド(イベントレートは激しい戦闘中でも秒間数件、Status.json は約1秒間隔で数百バイトの書き換え)。重い処理(翻訳推論、TTS)は別プロセスとして HTTP 越しに呼び出す構成。

## 最終アーキテクチャ

全体構成は以下で確定:

1. **Rust 製カーネル(wasmtime 同梱)** — プラグインは WASM として wasmtime 上で実行される
2. **ドライバ層** — 特権 capability を持つ層。プラグインが直接触れない資源へのアクセスを仲介する
3. **同梱 HTTP Driver** — HTTP アクセスを提供する標準ドライバ
4. **チャネルドライバ** — プラグイン間通信はチャネルドライバ経由で行う
5. **配布形式** — マニフェスト + アーカイブ

> 注: 上記は構成要素レベルでの確定。各要素の詳細仕様は未決定(「未決定事項」を参照)。

### 経緯(却下・変更された案)

- 当初は「ヘッドレス Node デーモン + WebSocket/Unix ドメインソケットでイベント配信」が推奨されたが、最終的には wasmtime 入り Rust カーネル構成に発展した。
- Electron 一体型(EDMC 型の monolith)は却下。

## GUI

- **デーモン(カーネル)と GUI は分離する。** GUI はデーモンに WebSocket で接続する純粋なクライアント。
  - ダッシュボードを閉じていても音声通知等の監視・配信は生き続ける
  - GUI のクラッシュや作り直しが監視レイヤーに波及しない
- 実装順序: まず**ブラウザ版ダッシュボード**(デーモンが静的ファイルを配信し `localhost:<port>` で開く)で中身を作り、独立ウィンドウが欲しくなった時点で **Tauri** の薄い皮を被せる。この順なら手戻りがほぼゼロ。
- Electron ではなく Tauri を採用(OS の WebView を使うためフットプリントが小さい。Tauri 側の Rust は「ウィンドウを出して WebSocket に繋ぐだけ」の薄い層)。
  - 留意点: WebKitGTK は Chromium とレンダリング挙動が微妙に異なるため、WebGL を凝った用途に使う場合は動作確認が必要。
- フロントエンドは React(react-three-fiber による WebGL 背景などのリッチ表現を想定)。

## ファイル監視の制約(必須要件)

- Journal は Proton 環境下でも compatdata 以下の通常の ext4 上のファイルであり inotify は基本的に効くが、ゲーム側の書き込みバッファリングにより inotify イベントが期待通りに来ないケースが報告されている。
- したがって **ポーリングへのフォールバック経路を最初から設計に組み込むこと**(EDMC も watchdog でポーリングフォールバックを持つ。Rust なら notify クレートの PollWatcher)。

## 未決定事項(今後の設計課題)

以下は構成要素として採用が確定しているのみで、詳細仕様はまだ決めていない:

- ドライバの capability モデルの詳細(付与・宣言・検証方法)
- プラグインマニフェストのスキーマとアーカイブ形式
- チャネルドライバの通信セマンティクス(pub/sub か point-to-point か等)
- カーネル⇔GUI 間 WebSocket プロトコルの詳細
